apiVersion: v1
kind: Service
metadata:
  name: npsvc
spec:
  selector:
    app: nginx
  ports:
  - name: nginx
    protocol: TCP
    port: 80
    targetPort: 80
  - name: postgress
    protocol: TCP
    port: 5432
    targetPort: 5432
